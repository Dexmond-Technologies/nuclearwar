#pragma once
// Particle system stub (visual effects are integrated inline in Renderer for
// now) This file exists for future extension
class ParticleSystem {
public:
  void update(float dt) {}
  void draw() {}
};
